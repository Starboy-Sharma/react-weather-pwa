import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));

/**
 * @Todos
 * [] Create a custome hook
 * [] On click change temprature from F to C
 * [] Write a Readme
 * [] Run app in offline state as well
 * [] About me page add.
 */

/**
 * App Feature
 * 1. Animated Icons of weather
 * 2. Temprature from F to C.
 * 3. Dynamic Background Images
 * 4. Show weather report using user geolocation
 * 5. Web can be run in offline state as well.
 */
